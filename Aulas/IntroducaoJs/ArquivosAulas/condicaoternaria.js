//resumindo um IF

// quando um IF  é simples ou seja tem apenas duas verificações .
// ? resultado da primeira condição
// : Resultado do que não é satisfatorio da nossa condição

var sexo = "m";


var retorno = (sexo === 'm') ?  'Masculino' : 'Feminino'

console.log(retorno);

